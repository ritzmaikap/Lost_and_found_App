<?xml version="1.0" encoding="utf-8"?>
<!-- Detail screen that appears after clicking an item from the list -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="22dp"
    android:background="#FFFFFF">

    <!-- Back button to return to item list -->
    <Button
        android:id="@+id/btnBackToList"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="BACK" />

    <!-- Uploaded image of lost/found item -->
    <ImageView
        android:id="@+id/imgItemDetail"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="16dp"
        android:background="#DDDDDD"
        android:scaleType="centerCrop"
        android:contentDescription="Uploaded item image" />

    <!-- Item details similar to the remove-item wireframe -->
    <TextView
        android:id="@+id/txtItemDetails"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:textSize="18sp"
        android:text="Item details will appear here" />

    <!-- Remove button -->
    <Button
        android:id="@+id/btnRemove"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="REMOVE" />

</LinearLayout>